<%= form_for @listing do |f| %>
  <%= f.label :card %>:
  <%= f.text_field :card %><br />

  <%= f.label :series %>:
  <%= f.text_field :series %><br />

  <%= f.label :number %>:
  <%= f.text_field :number %><br />

  <%= f.label :quantity %>:
  <%= f.number_field :quantity %><br />

  <%= f.label :color %>:
  <%= f.text_field :color %><br />

  <%= f.label :condition %>:
  <%= f.text_field :condition %><br />

  <%= f.submit %>
<% end %>

<%# add something to listings like "notes" where you can put additional stuff, and add to the template a place
where notes render.  so i could add some stuff, espeically to lots and all that %>